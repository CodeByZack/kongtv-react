import React from 'react';
import Icon from '../icon';
import './style.less';

const Loading = props => {
  return (
    <div className="loading">
      <Icon type="loading" />
    </div>
  );
};
export default Loading;
